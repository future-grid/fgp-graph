import Dygraph from 'dygraphs';


export class GraphInteractions {

    static mouseUp = (event, graph, context) => {
        console.debug("mouse up");
    }

    static mouseDown = (event, graph, context) => {
        console.debug("mouse down");
    }

}